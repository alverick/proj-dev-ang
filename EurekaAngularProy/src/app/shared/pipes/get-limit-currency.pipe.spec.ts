import { GetLimitCurrencyPipe } from './get-limit-currency.pipe';

describe('GetLimitCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLimitCurrencyPipe();
    void expect(pipe).toBeTruthy();
  });
});
